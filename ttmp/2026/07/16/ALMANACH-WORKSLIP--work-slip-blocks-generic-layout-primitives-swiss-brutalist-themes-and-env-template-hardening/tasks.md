# Tasks

## TODO

- [x] Phase 1: remove {{$ENV}} from template.go; update template tests; add server-boundary no-resolution test; update layout-dsl-reference doc <!-- t:s06o -->
- [ ] Phase 2: add qrcode-generator dep; implement web/src/blocks/slip/ (text, banner, rule, space, row, kv, list, checks, writein, qr, bars, table); register via mergeBlockRegistries; editor palette + generic JSON editor fallback; node tests; rebuild SPA + headless smoke render <!-- t:xtye -->
- [ ] Phase 3: proto ThemeTokens + regen; display/h1/h2/micro presets; token accessors with fallbacks; swiss/brutalist/terminal themes; Archivo subset if downloadable (else DejaVu Sans); verify render <!-- t:w279 -->
- [ ] Phase 4: example layouts 10-14 (job slip, decision sheet, triage card, focus card, morning digest); headless renders + screenshots; physical print; update help docs <!-- t:lbno -->
